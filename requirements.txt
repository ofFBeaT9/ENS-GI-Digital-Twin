# ENS-GI Digital Twin — Dependencies
# ===================================
# Installation: pip install -r requirements.txt

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
dataclasses>=0.6  # For Python <3.7

# Phase 3: PINN (Physics-Informed Neural Networks)
tensorflow>=2.15.0
tensorflow-probability>=0.22.0

# Phase 3: Bayesian Inference
pymc>=5.10.0  # Modern PyMC (v5+), replaces deprecated PyMC3
arviz>=0.12.0
pytensor>=2.18.0  # Modern backend, replaces Theano

# Visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Performance optimization (optional)
numba>=0.55.0  # JIT compilation
tqdm>=4.62.0   # Progress bars

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Documentation
sphinx>=4.5.0
sphinx-rtd-theme>=1.0.0

# Utilities
pandas>=1.3.0  # Data manipulation
h5py>=3.6.0    # Model serialization

# Alternative deep learning (optional, use instead of TensorFlow)
# torch>=2.0.0
# torchvision>=0.15.0

# Hardware simulation (optional)
# ngspice (system package, not pip)
# Install separately: https://ngspice.sourceforge.io/

# Notes:
# - TensorFlow is required for PINN
# - PyMC3 is required for Bayesian inference
# - For GPU acceleration: install tensorflow-gpu instead of tensorflow
# - For Apple Silicon: use tensorflow-macos and tensorflow-metal
